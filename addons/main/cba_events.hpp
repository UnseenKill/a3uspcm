#define CBA_EVENT_CLIENT_BUYVEHICLEDIALOG_AFTER QUOTE(TRIPLES(PREFIX,events,clientBuyVehicleDialogAfter))
#define CBA_EVENT_CLIENT_BUYVEHICLEDIALOG_BEFORE QUOTE(TRIPLES(PREFIX,events,clientBuyVehicleDialogBefore))

#define CBA_EVENT_SERVER_SAVEGAME_AFTER QUOTE(TRIPLES(PREFIX,events,serverSaveGameAfter))
#define CBA_EVENT_SERVER_SAVEGAME_BEFORE QUOTE(TRIPLES(PREFIX,events,serverSaveGameBefore))

// Params: Object player, String gvarName, Any value
#define CBA_EVENT_MENU_SYNCGVAR QUOTE(TRIPLES(ADDON,events,syncGVAR))
